# Runtime / production dependencies for Xplore IT Hub
# Use conservative ranges to allow security/bugfix updates while avoiding breaking changes.

Django>=4.2,<5
djangorestframework>=3.14,<4
psycopg2-binary>=2.9
django-environ>=0.10
django-cors-headers>=4.0
channels>=3.0,<4
channels-redis>=4.0
celery>=5.2
redis>=4.5
django-storages[boto3]>=1.12
boto3>=1.26
asgiref>=3.6
uvicorn>=0.17
gunicorn>=20.1
whitenoise>=6.0
Pillow>=9.0
python-dotenv>=1.0
djangorestframework-simplejwt>=5.0

# Optional / useful libraries (uncomment when used):
# psycopg[binary]==2.9.6  # alternative exact pin for psycopg
reportlab>=4.4.5

